<odoo>

    <!-- Acción para los servicios -->
    <record id="action_services_room_item" model="ir.actions.act_window">
        <field name="name">Elementos de Servicios</field>
        <field name="res_model">services.room.item</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Acción para los muebles -->
    <record id="action_furniture_room_item" model="ir.actions.act_window">
        <field name="name">Elementos de Mobiliario</field>
        <field name="res_model">furniture.item</field>
        <field name="type">ir.actions.act_window</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>